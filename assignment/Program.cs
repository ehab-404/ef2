using Ass_EFC01.Contexts;
using assignment.Entities;
namespace assignment
{
    internal class Program
    {
        static void Main(string[] args)
        {

            //appling crud operations 

            AssEFContext db = new AssEFContext();

            // db.Topics.Add(new Topic() { Name = "backend" });
            // db.SaveChanges();

            // var topics = db.Topics.ToList();

            // var topic = db.Topics.FirstOrDefault(t => t.Name == "backend");
            // topic.Name = "back";

            //db.Topics.Update(topic);
            // // db.Topics.Remove(topic);

            // db.SaveChanges();













        }
    }
}
